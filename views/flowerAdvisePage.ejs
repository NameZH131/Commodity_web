<!DOCTYPE html>
<html lang="zh-CN">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>花礼品鉴</title>
    <link rel="stylesheet" href="stylesheets/flowerAdvisePage.css">
    <style>
        #wikiResults {
            width: 100%;
            height: 600px;
            /* 可根据需要调整高度 */
            border: 1px solid #ccc;
            margin-top: 20px;
            display: none;
            /* 默认隐藏 */
        }
    </style>
</head>

<body>
    <div class="container">
        <h1 class="title">花礼品鉴</h1>
        <p>输入您的问题或描述，我们将使用维基百科为您提供建议。</p>
        <input type="text" id="flowerQuestion" class="input-field" placeholder="请输入您的查询" onkeydown="checkEnter(event)">
        <button class="btn" onclick="getWikiContent()">获取建议</button>
        <button class="btn" onclick="goHome()">返回主页</button>

        <iframe id="wikiResults" src="" frameborder="0" style="display:none;"></iframe>
    </div>

    <script>
        function getWikiContent() {
            var question = document.getElementById('flowerQuestion').value;
            if (question) {
                // 构建维基百科页面 URL
                var wikiUrl = 'https://zh.wikipedia.org/wiki/' + encodeURIComponent(question);

                // 将 URL 设置为 iframe 的 src
                var iframe = document.getElementById('wikiResults');
                iframe.src = wikiUrl;
                iframe.style.display = 'block'; // 显示 iframe

                // 设置容器宽度
                document.querySelector('.container').style.width = '100%'; // 修改为80%
                document.querySelector('.container').style.height = '100%'; // 修改为80%
            } else {
                alert("请输入查询内容。");
            }
        }

        function checkEnter(event) {
            if (event.key === 'Enter') {
                getWikiContent(); // 调用搜索函数
            }
        }

        function goHome() {
            window.location.href = 'index'; // 修改为您的主页链接
        }
    </script>
</body>

</html>